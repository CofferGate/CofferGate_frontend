# 작업 규칙

- 사용자가 명시적으로 요청하지 않는 한 `npm run dev`와 `npm run build`를 실행하지 않는다.
- 개발 서버를 자동으로 실행하거나 브라우저 검증을 하지 않는다.
- 디자인 수정은 필요한 파일만 변경하고 변경 내역을 보고한 뒤 종료한다.
- 장시간 실행되는 프로세스나 서버를 백그라운드에서 시작하지 않는다.
- 검증이 필요하면 직접 실행하지 말고 사용자에게 실행 방법만 알려준다.
